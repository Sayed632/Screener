name: Run Screener Automated Sync

on:
  schedule:
    - cron: '30 12 * * 1-5'
  workflow_dispatch:

jobs:
  build_and_run:
    runs-on: ubuntu-latest
    
    # 🔐 CRITICAL FIX: Explicitly grant write permissions to the automation token
    permissions:
      contents: write

    steps:
    - name: Checkout Code Repository
      uses: actions/checkout@v3
      with:
        # Uses the default token with the write permissions specified above
        token: ${{ secrets.GITHUB_TOKEN }} 

    - name: Set Up Python Environment
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Runtime Dependencies
      run: |
        pip install requests beautifulsoup4 pandas matplotlib

    - name: Execute Screener Scanner Core
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        MY_CHAT_ID: ${{ secrets.MY_CHAT_ID }}
        SCREENER_USERNAME: ${{ secrets.SCREENER_USERNAME }}
        SCREENER_PASSWORD: ${{ secrets.SCREENER_PASSWORD }}
      run: |
        python scanner.py

    - name: Save Updated History File Back to Repository
      run: |
        git config --global user.name 'GitHub Automation Bot'
        git config --global user.email 'actions@github.com'
        touch holdings_history.json
        git add holdings_history.json
        git commit -m "Sync updated holding pattern tracking cache [skip ci]" || exit 0
        git push
